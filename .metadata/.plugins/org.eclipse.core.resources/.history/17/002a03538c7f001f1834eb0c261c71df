package c06;

//메소드
//메소드는 객체의 동작(기능)에 해당하는 중괄호{}블록을 말합니다.
//메소드를 호출하면 중괄호 블록에 있는 모든 코드들이 일괄적으로 실행됩니다.
public class Method {
	//메소드의 기본 문법
	/*
	 * 리턴타입 메소드이름([매개변수선언,...]){
	 * 	실행할 코드를 작성하는 곳
	 * }
	 */
	int plus(int x,int y) {
		int result=x+y;
	}

}
